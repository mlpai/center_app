import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    shadow : {
        elevation: 10,
        shadowColor: '#52006A',
        shadowOffset : {
            width : 5,
            height : 5
        },
    }
})

export default globalStyles;